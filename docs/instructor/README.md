# Instructor-supplied material

Put the instructor's original `workflow.py` here for reference.

It is kept separate from `scripts/` deliberately: `scripts/` is your pipeline,
which you must be able to defend line by line. This folder is what you were
given. Keeping the boundary visible makes it clear which decisions are yours.
